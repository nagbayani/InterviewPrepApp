/** Rendering Tests:
 *    Test if DecksWrapper renders the correct number of DeckLink components based on the passed decks prop.
 *    Test if DecksWrapper displays the "Filter" and "Add Deck" buttons.
 *    Test if DecksWrapper handles an empty decks prop gracefully (e.g., display a message like "No decks available").
 */

/** State Management Tests:
 *    Test that DecksWrapper correctly updates the Zustand store (setDecks) when new deck data is passed.
 *    Test that DecksWrapper maintains the correct state for loading and error conditions.
 */

/** Interaction Tests:
 *    Test the interaction with the "Filter" dropdown menu (mocking or simulating a filter action).
 *    Test the AddDeckModal component’s behavior when clicking the "Add Deck" button.
 */

/** Integration Tests:
 *    Test that DecksWrapper correctly passes the necessary props to each DeckLink component and renders them properly.
 *    Ensure that interactions with DeckLink components update the Zustand store correctly.
 */
