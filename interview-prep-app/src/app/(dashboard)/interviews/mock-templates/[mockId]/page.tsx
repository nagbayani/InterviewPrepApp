import React from "react";
import { currentUser } from "@/lib/auth";

const MockIdPage = ({ params }: { params: { mockId: string } }) => {
  
  return (
    <div>
      <h1>MockIdPage</h1>
    </div>
  );
};

export default MockIdPage;
